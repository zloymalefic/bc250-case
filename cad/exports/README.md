# Validation exports only

There is currently no printable case release in this directory.

The retained files are small calibration objects:

- `fit-calibration-coupon-v0.1.stl` — M3/M4 holes, heat-set pilots, and snap clearances;
- `chassis-joint-coupon-0.25mm-v0.1.stl`;
- `chassis-joint-coupon-0.35mm-v0.1.stl`;
- `chassis-joint-coupon-0.45mm-v0.1.stl`.

Do not combine these into a case assembly. Release parts will appear under `exports/release/<version>/` only after a complete master-assembly check.

